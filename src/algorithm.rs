extern crate etig;

use etig::samarth;
use etig::praneel;
use etig::vaibhav;
use etig::eti11;
use etig::yash;
use etig::d_raj_radio_coloring;
use etig::d_raj_graph_coloring;
use etig::rahlav;
use etig::apurv;
use etig::rahul_073;
use etig::kamal;
use etig::akash_floydwarshall;
use etig::sameer;
use etig::hima;
use etig::cycle_detection;
use etig::manav_final;
use etig::bipartite_43_181;
use etig::cyc_det_directed_y2k;

fn bellman_ford_110(local_adj_034: &mut [[i32; 350]; 350], local_nodes_034:i32) {
    samarth::bellman_ford_110(&mut gra,na);
}

fn dfsutil_034(local_adj_034: &mut [[i32; 350]; 350], local_nodes_034:i32) {
    praneel::dfsutil_034(&mut gra, na);
}

fn solve_bfs(local_adj_034: &mut [[i32; 350]; 350], local_nodes_034:i32) {
    vaibhav::solve_bfs(&mut gra, na);
}

fn radiocoloring(local_adj_034: &mut [[i32; 350]; 350], local_nodes_034:i32) {
    d_raj_radio_coloring::radiocoloring(&mut gra, na);
}

fn graphcoloring(local_adj_034: &mut [[i32; 350]; 350], local_nodes_034:i32) {
   d_raj_graph_coloring::graphcoloring(&mut gra, na);
}

fn solve_062(local_adj_034: &mut [[i32; 350]; 350], local_nodes_034:i32) {
    rahlav::solve_062(&mut gra, na);
}

fn caller_105(local_adj_034: &mut [[i32; 350]; 350], local_nodes_034:i32) {
    yash::caller_105(&mut gra, na);
}

fn input(local_adj_034: &mut [[i32; 350]; 350], local_nodes_034:i32) {
    eti11::input(&mut gra,na);
}

fn count_ways(local_adj_034: &mut [[i32; 350]; 350], local_nodes_034:i32) {
    apurv::count_ways(&mut gra, na);
}

fn conn_comp_073(local_adj_034: &mut [[i32; 350]; 350], local_nodes_034:i32) {
    rahul_073::conn_comp_073(&mut gra, na);
}

fn caller_54(local_adj_034: &mut [[i32; 350]; 350], local_nodes_034:i32) {
    kamal::caller_54(&mut gra, na);
} 

fn floydwarshall_207(local_adj_034: &mut [[i32; 350]; 350], local_nodes_034:i32) {
    akash_floydwarshall::floydwarshall_207(&mut gra, na);
}

fn johnson(local_adj_034: &mut [[i32; 350]; 350], local_nodes_034:i32) {
    sameer::johnson(&mut gra, na);
}

fn func(local_adj_034: &mut [[i32; 350]; 350], local_nodes_034:i32) {
    hima::func(&mut gra, na);
}

fn cycle_detection(local_adj_034: &mut [[i32; 350]; 350], local_nodes_034:i32) {
    cycle_detection::cycle_detection(&mut gra, na);
}

fn solve_173(local_adj_034: &mut [[i32; 350]; 350], local_nodes_034:i32) {
    manav_final::solve_173(&mut gra, na);
}

fn check_bipartite(local_adj_034: &mut [[i32; 350]; 350], local_nodes_034:i32) {
    bipartite_43_181::check_bipartite(&mut gra, na);
}

fn cyc_det_directed_y2k(local_adj_034: &mut [[i32; 350]; 350], local_nodes_034:i32) {
    cyc_det_directed_y2k::cyc_det_directed_y2k(&mut gra, na);
}








 

